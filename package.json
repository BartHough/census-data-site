{
  "name": "census-data-site",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "google-maps-react": "^2.0.2",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-geocode": "^0.2.0",
    "react-scripts": "3.2.0",
    "react-select": "^3.0.8"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy-prod": "aws s3 cp --recursive ./build s3://census-data-prod",
    "deploy-beta": "aws s3 cp --recursive ./build s3://census-data-beta",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
